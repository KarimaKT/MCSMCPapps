# Microsoft 365 Agents Toolkit project file.
# Open this folder in VS Code with the Microsoft 365 Agents Toolkit
# extension installed; Provision and Publish from the toolkit pane.
# Docs: https://aka.ms/m365-agents-toolkit-docs

version: v1.7

projectId: 4f1d8e2c-6a3b-4e9d-9f15-c2a7b3d5e8f1

environmentFolderPath: ./env

# Provision creates the Microsoft 365 app entry, registers the
# declarative agent, and prepares an app package zip.
provision:
  - uses: teamsApp/create
    with:
      name: Eurozone Analyst (MCSMCPapps)
    writeToEnvironmentFile:
      teamsAppId: TEAMS_APP_ID

  - uses: teamsApp/zipAppPackage
    with:
      manifestPath: ./appPackage/manifest.json
      outputZipPath: ./appPackage/build/appPackage.${{TEAMSFX_ENV}}.zip
      outputFolder: ./appPackage/build

  - uses: teamsApp/validateAppPackage
    with:
      appPackagePath: ./appPackage/build/appPackage.${{TEAMSFX_ENV}}.zip

  - uses: teamsApp/update
    with:
      appPackagePath: ./appPackage/build/appPackage.${{TEAMSFX_ENV}}.zip

deploy: []

# Publish uploads the app to the Microsoft 365 tenant for sideload.
# In CDX you can run this freely; in production tenants this requires
# admin approval in Teams Admin Center.
publish:
  - uses: teamsApp/zipAppPackage
    with:
      manifestPath: ./appPackage/manifest.json
      outputZipPath: ./appPackage/build/appPackage.${{TEAMSFX_ENV}}.zip
      outputFolder: ./appPackage/build

  - uses: teamsApp/validateAppPackage
    with:
      appPackagePath: ./appPackage/build/appPackage.${{TEAMSFX_ENV}}.zip

  - uses: teamsApp/update
    with:
      appPackagePath: ./appPackage/build/appPackage.${{TEAMSFX_ENV}}.zip

  - uses: teamsApp/publishAppPackage
    with:
      appPackagePath: ./appPackage/build/appPackage.${{TEAMSFX_ENV}}.zip
    writeToEnvironmentFile:
      publishedAppId: TEAMS_APP_PUBLISHED_APP_ID
